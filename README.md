# Custom API Gateway (with Rate Limiting & Observability)
A custom API gateway that sits in front of microservices, handling cross-cutting concerns. Core capabilities include traffic routing, load balancing, API key auth, rate limiting, and centralized metrics/logging
